import {
  sortText as text,
  sortNumeric as numeric,
  sortCurrency as currency,
  sortDate as date
} from './utils/sorters';

const jsort = {
  text,
  numeric,
  currency,
  date
};

export default jsort;
